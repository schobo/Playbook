---
- name: Chạy ls -la trên remote host
  hosts: all
  gather_facts: false
  tasks:
    - name: Thực thi ls -la
      shell: ls -la
      register: ls_output

    - debug:
        var: ls_output.stdout
